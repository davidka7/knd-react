const PROJECT_INITIAL_STATE = []

export const user_projectReducer = (state = PROJECT_INITIAL_STATE, action) => {
    switch (action.type) {
    
        default:
            return state;
    }
    
}